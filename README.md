# AuthFlows

AuthFlows is a full-stack authentication and account-management application built with the Next.js App Router. It implements a multi-step sign-up and sign-in experience with email verification, login OTP verification, password reset, protected dashboard access, and an editable user profile.

This project is designed to show how a modern authentication flow can be assembled in a structured way using server-rendered pages, server actions, JWT-based sessions, email delivery, and a PostgreSQL-backed data model.

## What The System Does

At a high level, the system allows a user to:

- create a new account with a name, email, and password
- receive an email verification link after registration
- verify the newly created account
- be automatically logged in after successful email verification
- log in securely with a password plus a one-time verification code
- request a password reset email and create a new password
- access a protected dashboard only after authentication
- update limited profile information from the dashboard

The application is intentionally opinionated about access control. Unauthenticated users are routed toward the login experience, while authenticated users are allowed into the dashboard and protected areas.

## Core Features

### 1. Account Registration

Users can create an account through the sign-up flow. During sign-up:

- the form is validated on the server
- duplicate email addresses are rejected
- the password is hashed with `argon2`
- a new user row is created in PostgreSQL through Prisma
- an email verification token is generated and stored
- a verification email is sent through Resend

This means a user account is created first, but it is not considered active until email verification succeeds.

### 2. Email Verification

After registration, the user receives a verification link. When they open it:

- the token is validated
- the token expiry is checked
- the user’s `emailVerified` field is updated
- a welcome email is sent on first successful verification
- the user is automatically signed in
- the user is redirected directly to the dashboard

This implementation is an important UX application that turns verification into the completion step of onboarding.

### 3. Password Login With OTP Verification

This project does not stop at basic email/password sign-in. Instead, it adds a second layer of login verification:

- the user enters email and password
- if the password is correct and the email is verified, a one-time code is generated
- the code is stored in the database with an expiry time
- the code is emailed to the user
- the user is redirected to the `/verify-login` page
- the user must enter the correct 6-digit code to finish the login

This creates a lightweight two-step login flow that improves security without introducing an external authenticator app.

### 4. Password Reset

If a user forgets their password, they can request a reset link:

- the app generates a password reset token
- the token is emailed to the user
- the user opens the reset-password page with the token
- the app verifies the token and expiry
- the new password is validated and hashed
- the user’s password hash is updated in the database

### 5. Protected Dashboard

The dashboard is a protected area available only to authenticated users. It currently shows:

- a personalized welcome headline
- the user’s name
- the user’s email address
- login verification status
- the current profile-picture state

The dashboard is intentionally focused on user-relevant information rather than raw internal identifiers.

### 6. Editable User Profile

Inside the dashboard, users can update a limited set of profile fields:

- full name
- age
- gender
- profile picture

The system is intentionally restrictive. Users cannot edit arbitrary account data. Only explicitly permitted fields are accepted by the server action that updates the profile.

### 7. Profile Picture Upload

Users can change their profile picture from the dashboard using:

- drag and drop
- upload from computer

The uploaded image is resized and compressed in the browser before being submitted. This keeps the payload under Next.js Server Action body-size limits and avoids unnecessarily large requests.

If the user does not provide a custom image, the dashboard falls back to a default avatar generated by the app.

### 8. Welcome Email

When a newly registered user successfully verifies their account for the first time, the system sends a welcome email. This is important because it ties the message to actual account activation, not just account creation.

That makes the email more meaningful and prevents welcome emails from being sent to accounts that were never verified.

## Authentication And Access Flow

The current end-to-end flow looks like this:

### New User Flow

1. User signs up.
2. App creates the account with `emailVerified = null`.
3. App sends an email verification link.
4. User opens the link.
5. App verifies the token.
6. App marks the email as verified.
7. App sends the welcome email.
8. App creates a session.
9. User is redirected to `/dashboard`.

### Returning User Login Flow

1. User enters email and password.
2. App validates credentials.
3. App generates a login OTP and emails it.
4. User is redirected to `/verify-login`.
5. User enters the 6-digit code.
6. App validates the OTP.
7. App creates a session.
8. User is redirected to `/dashboard`.

### Password Reset Flow

1. User requests password reset.
2. App generates a reset token.
3. App emails the reset link.
4. User opens the link.
5. App validates the token.
6. User submits a new password.
7. App hashes and stores the new password.

## How The System Works Internally

### Session Management

Authentication is managed with NextAuth/Auth.js using a JWT session strategy. The app exports:

- `auth`
- `signIn`
- `signOut`
- `handlers`

from [`auth.ts`](./auth.ts).

The project uses a custom `Credentials` provider with multiple sign-in paths:

- standard email/password login
- OTP-completed login using `loginToken`
- post-verification auto-login using `emailVerificationToken`

This gives the app one centralized authentication layer while still supporting multiple entry points into the session system.

### Route Protection

The root route `/` redirects users based on session state:

- authenticated users go to `/dashboard`
- unauthenticated users go to `/login`

The app also uses [`middleware.ts`](./middleware.ts) to guard protected routes more broadly. Public routes such as login, signup, verification, and password reset remain accessible without a session, while protected routes require a valid auth token.

### Database Access

Prisma is used as the ORM layer. The Prisma Client is initialized in [`lib/prisma.ts`](./lib/prisma.ts) using:

- `@prisma/client`
- `@prisma/adapter-pg`
- `pg`

The project uses PostgreSQL as its database backend.

### Email Delivery

Transactional emails are sent through Resend in [`lib/email.ts`](./lib/email.ts). The project currently sends:

- email verification emails
- login OTP emails
- password reset emails
- welcome emails

### Validation

The project uses `zod` to validate form input. Validation logic currently lives in:

- [`lib/validations/signup.ts`](./lib/validations/signup.ts)
- [`lib/validations/login.ts`](./lib/validations/login.ts)

This keeps validation rules close to the authentication domain and avoids fragile client-only validation.

## Tech Stack

### Frontend

- **Next.js 16.2.0**
- **React 19.2.4**
- **TypeScript**
- **App Router**
- **Server Components + Client Components**
- **CSS via `app/globals.css`**
- **Lucide React** for icons

### Backend / Server Logic

- **Next.js Server Actions**
- **Next.js Route Handlers**
- **NextAuth/Auth.js v5 beta**

### Authentication / Security

- **Credentials provider**
- **JWT session strategy**
- **argon2** for password hashing
- custom email-verification tokens
- custom login OTP tokens
- password-reset tokens

### Database

- **PostgreSQL**
- **Prisma ORM**
- **@prisma/adapter-pg**
- **pg**

### Email / Messaging

- **Resend**

### Validation / Utility

- **zod**

### Tooling

- **ESLint**
- **TypeScript**
- **Prisma migrations**

## Data Model

The main entities in [`prisma/schema.prisma`](./prisma/schema.prisma) are:

### `User`

Stores account and profile data, including:

- `name`
- `email`
- `passwordHash`
- `age`
- `gender`
- `image`
- `emailVerified`
- `phoneVerified`

### `EmailVerificationToken`

Stores the token used to verify a newly created account.

### `LoginVerificationToken`

Stores the login OTP code and the login token used during the second step of sign-in.

### `PasswordResetToken`

Stores reset-password links and expirations.

## Project Structure

Key files and folders:

### App Routes

- [`app/page.tsx`](./app/page.tsx)  
  Root redirect logic

- [`app/signup/page.tsx`](./app/signup/page.tsx)  
  Registration UI

- [`app/signup/actions.ts`](./app/signup/actions.ts)  
  Registration server logic

- [`app/login/page.tsx`](./app/login/page.tsx)  
  Login UI

- [`app/login/actions.ts`](./app/login/actions.ts)  
  Password validation and OTP generation

- [`app/verify-login/page.tsx`](./app/verify-login/page.tsx)  
  OTP verification UI

- [`app/verify-login/actions.ts`](./app/verify-login/actions.ts)  
  OTP verification and resend logic

- [`app/verify-email/page.tsx`](./app/verify-email/page.tsx)  
  Public verification entry route

- [`app/api/verify-email/route.ts`](./app/api/verify-email/route.ts)  
  Verifies email, sends welcome email, signs in user, redirects to dashboard

- [`app/forgot-password/page.tsx`](./app/forgot-password/page.tsx)  
  Password-reset request UI

- [`app/forgot-password/actions.ts`](./app/forgot-password/actions.ts)  
  Password-reset token generation and email sending

- [`app/reset-password/page.tsx`](./app/reset-password/page.tsx)  
  Reset-password token validation route

- [`app/reset-password/ResetPasswordForm.tsx`](./app/reset-password/ResetPasswordForm.tsx)  
  New-password form UI

- [`app/reset-password/actions.ts`](./app/reset-password/actions.ts)  
  Password update logic

- [`app/dashboard/page.tsx`](./app/dashboard/page.tsx)  
  Protected dashboard

- [`app/dashboard/profile-form.tsx`](./app/dashboard/profile-form.tsx)  
  Editable profile form

- [`app/dashboard/actions.ts`](./app/dashboard/actions.ts)  
  Profile update logic

### Shared Logic

- [`auth.ts`](./auth.ts)  
  Central authentication config

- [`middleware.ts`](./middleware.ts)  
  Route protection

- [`lib/prisma.ts`](./lib/prisma.ts)  
  Prisma client initialization

- [`lib/email.ts`](./lib/email.ts)  
  Resend email functions

- [`lib/avatar.ts`](./lib/avatar.ts)  
  Default avatar generation

## Environment Variables

This project expects environment variables such as:

- `DATABASE_URL`
- `AUTH_SECRET`
- `RESEND_API_KEY`
- `EMAIL_FROM`
- `APP_URL`

These are used for:

- database connectivity
- auth token signing
- email delivery
- URL generation for verification and reset links

## Running The Project

Install dependencies:

```bash
npm install
```

Start the development server:

```bash
npm run dev
```

Run linting:

```bash
npm run lint
```

Build for production:

```bash
npm run build
```

Start the production server:

```bash
npm run start
```

## Database Setup

Prisma schema and migrations live in the [`prisma`](./prisma) folder.

When the schema changes, regenerate the Prisma client:

```bash
prisma generate
```

If your environment supports Prisma migration commands, apply schema changes through migrations so the database and generated client stay in sync.

## Design Notes

This project intentionally blends a polished UI with a relatively strict authentication model:

- the login flow is not just password-only
- email verification is a real activation step
- dashboard access is protected
- profile edits are limited and explicit
- onboarding is completed by auto-login after verification

That makes the system a good example of an application that balances user experience with layered account protection.

## Current Strengths

- multi-step authentication flow
- verified-email onboarding
- automatic post-verification sign-in
- protected dashboard routing
- transactional email support
- server-side validation and token handling
- profile editing with controlled field permissions
- image upload flow that respects Server Action size limits

## Future Enhancements

Possible next improvements could include:

- stronger audit logging for auth events
- rate limiting for OTP and verification actions
- image storage in object storage instead of data URLs
- account linking or social sign-in
- richer dashboard analytics or activity history
- more detailed account settings management

## Summary

AuthFlows is more than a simple sign-in form. It is a complete authentication workflow application with:

- registration
- verification
- auto-login on activation
- login OTP verification
- password reset
- protected dashboard access
- editable user profile
- transactional email support

It combines Next.js App Router patterns, Auth.js session handling, Prisma-backed persistence, Resend-based email delivery, and layered access control into a cohesive full-stack authentication system.
